import React from 'react';

// Google AdSense has been removed from the app.
// This component is intentionally left as a no-op to avoid breaking imports.
export const PublicAdsDisplay: React.FC = () => null;
